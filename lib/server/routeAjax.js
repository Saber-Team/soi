/**
 * @fileoverview 根据server节点的rewrite配置, 对ajax的请求采取静态数据返回
 * @author AceMood(zmike86)
 * @email zmike86@gmail.com
 */

'use strict';



module.exports = function ajax (conf) {
    var rewrite = conf.rewrite || {};

    return {

    };
};