/*var jsdom = require('jsdom-nocontextifiy');
var path = require('path');
var utils = require('../../lib/utils');

var f = path.join(__dirname + '/test.html');

jsdom.env({
  html: f,
  scripts: [],
  done: function(errors, window) {
    debugger;
    console.log(window.list);
  }
});*/